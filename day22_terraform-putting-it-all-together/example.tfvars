# example.tfvars
# Copy this file and rename to terraform.tfvars for local development only.
# NEVER commit terraform.tfvars — it is in .gitignore.
# In CI/CD, set these as environment variables: TF_VAR_db_username, TF_VAR_db_password

db_username = "replace_me"
db_password = "replace_me_with_strong_password"
